/**
 * Scroll distance past which the welcome page is no longer "at the top", and the scroll cue
 * hides. A leaf so the inline body script (`seo/staticPaintBody.ts`) hides the prerendered
 * cue at the same point.
 */
export const AT_TOP_THRESHOLD_PX = 24;
