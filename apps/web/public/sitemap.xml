<?xml version="1.0" encoding="UTF-8"?>
<!--
  One URL, on purpose. `/` is the canonical landing page; `/welcome` serves the
  same content and canonicalises to it, and the workspace routes are an empty
  app shell with nothing to rank.

  Static for now. It moves into the prerender script when that lands, so the
  origin is written down in one place rather than three.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://oristudio.dev/</loc>
    <changefreq>weekly</changefreq>
    <priority>1.0</priority>
  </url>
</urlset>
